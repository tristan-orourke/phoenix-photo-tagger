{
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Edit|Write",
				"hooks": [
					{
						"type": "command",
						"command": "INPUT=$(cat); echo \"$INPUT\" | jq -r '.tool_input.file_path // empty' | grep -qE '\\.(ex|exs|heex)$' && docker compose -f docker-compose-dev.yml run dev_app mix format 2>&1 | head -20 && docker compose -f docker-compose-dev.yml run dev_app mix compile 2>&1 | head -20 || true",
						"timeout": 120,
						"statusMessage": "Formatting and compiling..."
					}
				]
			}
		]
	}
}
